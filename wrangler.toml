# wrangler.toml — Configuration Cloudflare Pages
# Les bindings D1 et R2 se configurent dans le Dashboard:
# Pages → Settings → Functions → Bindings
name = "geotopo-pro"
pages_build_output_dir = "public"
compatibility_date = "2024-09-23"
